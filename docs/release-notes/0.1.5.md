What's new in PaintNode 0.1.5

- Expanded the Image menu with new document-level workflows for image size, canvas size, duplicate document, trim, threshold, levels, upscale, and AI auto-adjust.
- Added richer native menu wiring so desktop Image menu actions match the in-app menu commands.
- Improved adjustment engine coverage for the new image workflow commands.
- Fixed the macOS app icon bundle so the packaged desktop app uses the corrected PaintNode icons.
