What's new in PaintNode 0.2.4

- PaintNode now checks for application updates whenever it starts, so new releases are discovered without opening Check for Updates.
- Codex and Claude managed-runtime update checks remain throttled separately to reduce unnecessary provider download checks.
