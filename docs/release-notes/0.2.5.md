What's new in PaintNode 0.2.5

- Added Grok as an AI Director and image provider for generation, editing, asset extraction, upscaling, and workflow runs through your existing Grok installation.
- Expanded the node workflow board with AI-assisted creation and revision, per-node AI roles, typed connections, selective execution, candidate review, and reusable generated outputs.
- Added a denser workflow authoring workspace with a node palette, map, properties, undo and recovery, task locking, and explicit save locations for new boards.
- Improved workflow reliability across cancellation, generation inputs, readiness checks, task details, viewport navigation, and preserving results while a board is edited.
- Added a native launch screen and refined provider setup, detection, capability discovery, and per-run options.
- Updated PaintNode-managed Codex and Claude packages to runtime protocol 5 for structured workflow Director output and stronger executable trust checks.
