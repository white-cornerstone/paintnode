What's new in PaintNode 0.2.1

- Fixed PaintNode-managed Codex and Claude setup. Provider update manifests from GitHub Releases now load correctly, so the managed runtime can be downloaded and installed.
