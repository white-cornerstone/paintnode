What's new in PaintNode 0.1.6

- Improved native desktop menus so menu items enable and disable based on the current document, selection, tool, and editing state.
- Kept the macOS native menu in sync with the in-app menu for document and image workflow commands.
- Improved update relaunch behavior on macOS so PaintNode comes back into focus after installing an update.
- Fixed Codex CLI detection and AI runs in production macOS builds when the Codex launcher needs Node from Homebrew or another common user CLI path.
