# COPY OUTSIDE REPOSITORY — private facilitator intervention log

> PRIVATE ONLY. Never complete this file inside the repository.

| Time | Task | Action | Visible label | Expected trigger | Observed trigger | Reset time | Hint ID | Exact hint used | Takeover action ID | Exact takeover action | Assist ordinal | Assist event type | Deviation ID | Session validity effect |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | ---: | --- | --- | --- |
| | | setup / reset / hint / takeover | | | | | | | | | | | | |

Every scenario change must be visible, narrated, timestamped, and reset to
`Standard checkpoint` before the participant retries.

For a hint, record the checkpoint ID with `-H1` or `-H2` as Hint ID and copy the
Exact hint used; record `N/A` in both takeover columns. For a takeover, record
`N/A` in both hint columns and copy the Takeover action ID and Exact takeover
action from `takeoverActions` in `facilitator-hints.json`. Assist ordinal is
task-local and increases only for an event with `assistIncrement: 1`. Use only
the closed Assist event type and Deviation ID values; copy the corresponding
Session validity effect exactly.
